apiVersion: v1
kind: Service
metadata:
  name: {{ include "if-demo.fullname" . }}
  labels:
    {{- include "if-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "if-demo.selectorLabels" . | nindent 4 }}
